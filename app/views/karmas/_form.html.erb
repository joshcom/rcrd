<%= form_for(@karma) do |f| %>

  <div id="tools">
    <%= f.submit 'Create Karma', :class => 'button' %>
    <%= link_to 'Back', karmas_path, :class => 'button' %>
  </div>  

  <% if @karma.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@karma.errors.count, "error") %> prohibited this karma from being saved:</h2>

      <ul>
      <% @karma.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :points %><br />
    <%= f.text_field :points %>
  </div>
<% end %>
